//Lorenzo Toscano enums
#ifndef ENUM
#define ENUM 10

enum squareColors { //BLACK=0 WHITE=1
 SQ_BLACK, SQ_WHITE
};

enum chipColor {      // value as int EMPTY==0 RED==1 BLACK==2
  EMPTY, RED, BLACK
};

enum rank {    // value as int NOTKING==0 KING==1
  NOTKING, KING
};

#endif